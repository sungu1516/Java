
public class A extends B {
	public A(int n) {
		super(n);
		B b = new B(); // 이를 의존 객체라고 한다. 
		
	}
	
	public A() {
		super(); // 굳이 선언되지 않아도 B의 기본 생성자는 호출이 된다!
		
		System.out.println("A의 기본 생성자입니다.");
	}
}
